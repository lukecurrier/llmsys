"def encode(message: str) -> str:\n    \"\"\"\n    Write a function that takes a message, and encodes in such a \n    way that it swaps case of all letters, replaces all vowels in \n    the message with the letter that appears 2 places ahead of that \n    vowel in the english alphabet. \n    Assume only letters. \n    \n    Examples:\n    >>> encode('test')\n    'TGST'\n    >>> encode('This is a message')\n    'tHKS KS C MGSSCGG'\n    \"\"\"\n    # TODO: Write your code here\n    # 1. Create a list of all the letters in the message\n    # 2. Create a list of all the vowels in the message\n    # 3. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 4. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 5. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 6. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 7. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 8. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 9. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 10. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 11. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 12. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 13. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 14. Create a list of all the letters that appear 2 places ahead of the vowels\n    # 15"